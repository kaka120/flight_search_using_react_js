export interface schemaRelatedToflight {
    flight_id: string,
    source: string,
    source_code: string,
    destination: string,
    destination_code: string,
    fare: Number,
    departs_at: string,
    arrives_at: string,
    SeatsAvailable:Number
}
